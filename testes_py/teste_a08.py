import math
# importar uma biblioteca inteira (from math import sqrt [para importar apenas uma funcionalidade])
import random
num = random.randint(1, 100)
raiz = math.sqrt(num)
print('A raiz do número {} é {:.2f}'.format(num, raiz))
